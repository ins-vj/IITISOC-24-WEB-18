import {Button} from "@nextui-org/react";
import styles from "./ButtonComponent.module.css";
export const ButtonComponent = () => {
    return (
        <Button color="primary" className={styles.special}>Join MEET now</Button>
    )
}